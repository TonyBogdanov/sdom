dist: trusty

language: php
php:
  - 7.0.27
  - 7.1.13
  - 7.2.1

before_install:
  - chmod +x ./bin/run-tests.sh
  - chmod +x ./bin/run-tests-with-coverage.sh

install:
  - composer install

script:
  - ./bin/run-tests-with-coverage.sh